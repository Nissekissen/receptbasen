private:
  name: private group
  public: false
  owner: one

public:
  name: public group
  public: true
  owner: one

# A second private group, unrelated to `private` — needed to test that an
# invite token minted for one private group can't be used to join another.
other_private:
  name: other private group
  public: false
  owner: two
